const quarterValue = (orgNumber) => {
    return orgNumber / 4
}
// Invoke the function and store the return value
const calculatedNumber = quarterValue(20)

// Log the return value
console.log(calculatedNumber);
